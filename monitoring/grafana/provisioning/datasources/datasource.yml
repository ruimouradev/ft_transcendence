# Read once at startup, connects grafana to prometheus.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # grafana queries prometheus, so it can stay unexposed
    access: proxy
    url: http://prometheus:9090
    isDefault: true
